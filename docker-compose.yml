services:
  web:
    build:
      context: ./
      dockerfile: ./web.Dockerfile
    ports:
      - "80:3000"
  websocket:
    build:
      context: ./
      dockerfile: ./websocket.Dockerfile
    ports:
      - "8080:8080"
